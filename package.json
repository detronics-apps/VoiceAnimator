{
  "name": "VoiceAnimator",
  "version": "1.3.2",
  "private": true,
  "type": "module",
  "description": "Turn script text into lip-sync mouth-shape timing, drive your own character artwork with it, and export a Rhubarb-compatible track.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python -m http.server 8847"
  }
}
