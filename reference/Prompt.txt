Build a new Detronics browser app called VoiceAnimator that does works like the following link https://github.com/danielswolf/rhubarb-lip-sync, that can swop out images of the mouth making different letters and sync it with scrip text. Use the detronics-app skill — scaffold with its script, read references/pitfalls.md before any viewport CSS or SVG, pure modules and tests first, then the UI. 